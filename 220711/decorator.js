/* function simpleDecorator(x) {
  console.log(
    `simpleDecorator의 실행 결과 Class ${x}를 인자로 받아 가공해줍니다.`
  );
  return newClass extends x {

  }
}

@simpleDecorator()
class Decorated {
  constructor(x, y) {
    this.plus = x + y;
    this.minus = x - y;
  }
}
 */
